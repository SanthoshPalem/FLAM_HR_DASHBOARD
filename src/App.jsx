import React from 'react';
import Home from './components/Home';

const App = () => {
  return (
    <div style={{background: 'var(--body_background)' ,color:'var(--body_color)'}}>
      <Home />
    </div>
  )
}

export default App
